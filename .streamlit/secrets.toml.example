# Local secrets template.
# Copy to .streamlit/secrets.toml (gitignored) for local runs, OR paste the same
# keys into Streamlit Community Cloud -> App -> Settings -> Secrets.

OPENAI_API_KEY = "sk-your-openai-key"

# Optional - only if you use the Anthropic provider.
# ANTHROPIC_API_KEY = "sk-ant-your-anthropic-key"
