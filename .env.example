# Copy to .env and fill in. Required for OpenAI embeddings + (optional) OpenAI chat.
OPENAI_API_KEY=sk-your-openai-key

# Optional: only needed if you select the Anthropic provider in the UI.
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
